var num = 1;
// Write a while loop that prints out
// only the even numbers from 1 to 10

while (num < 11) {
  //console.log("x is currently: " + num);
  if (num%2 === 0) {
    console.log(num);
  }
  num = num+1;

  // if (x===3) {
  //   console.log("X IS EQUAL TO THREE!");
  //   break;
  // }

  // console.log("x is still less than 5, adding 1 to x");

  // x = x+2;
}
